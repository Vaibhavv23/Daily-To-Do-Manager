<!-- Nav Bar Section -->
<%- include('../components/nav-bar-dashboard.ejs') %>

<!----------------SideBarSection---------------------->
<%- include('../components/side-bar-dashboard.ejs') %>

<div id="main">
    <h5>Welcome <%=name %></h5>
    <div class="card">
        
            <div class="card-header">
                <div class="row">
                    <div class="col-md-4">
                        <h5>Tasks</h5>
                    </div>
                    <div class="col-md-4">
                    </div>
                    <div class="col-md-4">
                        <a href="/tasks/add/goal/<%= goal_id %>">
                        <button class=" float-right btn btn-md btn-outline-success">
                            Add task
                        </button>
                        </a>
                    </div>
                </div>
                </div>
            <div class="card-body">
                <% if(tasks.length<1) { %>
                    <h6  class="nogoals" >
                        You have not created any tasks.Click on add tasks button at right side to create new tasks</h6>
                <% }else { %>
                <table class="table table-dark table-hover table-bordered">
                    <thead >
                        <tr>
                            <th>task Name</th>
                            <th>Priority</th>
                            <th>Task Type</th>
                            <th>Target </th>
                            <th>Edit</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% tasks.forEach(function(task){ %>
                            <td><a href="/tasks/<%= task._id %>" ><%= task.taskName %></a></td>
                                <td><%= task.priority %></td>
                                <td><%= task.taskType %></td>
                                <td><%= task.target %></td>
                                <td>
                                    <a href="/tasks/edit/<%= task._id %>" >
                                        <button class="btn btn-primary badge-pill" style="width:80px;"> 
                                            <i class="fa fa-edit"></i>
                                        </button>
                                    </a>
                                </td>
                                <td> 
                                   <form action="/tasks/<%= task._id %>/goals/<%=  %>" method="POST" id="delete-form">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button class="btn btn-danger badge-pill" style="width:80px;">
                                        <i class="fa fa-trash" aria-hidden="true"></i>
                                    </button>
                                </form>
                                </td>
                        <% }) %>
                        
                    </tbody>
                </table>
            
            </div>
            <% } %>
        
    </div>
    
</div>